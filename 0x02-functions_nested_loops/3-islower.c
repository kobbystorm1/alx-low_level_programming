#include "main.h"
/**
 * _islower - checks if character is a lower character
 *
 * Return: 0.
 * @c - placeholder for the character
 */
int _islower(int c)

{

	if ((c >= 'a') && (c <= 'z'))

	{
		return (1);
	}
	else
	{
		return (0);
	}

}
